
<form method="post" action="{{ path('app_agencia_delete', {'id': agencia.id}) }}" onsubmit="return confirm('Tem certeza que deseja excluir esta agência?');" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agencia.id) }}">
    <button type="submit" class="btn btn-sm btn-danger">Excluir</button>
</form> 