<form method="post" action="{{ path('app_pessoa_corretor_delete', {'id': pessoa_corretor.id}) }}" style="display:inline-block" data-confirm-delete>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pessoa_corretor.id) }}">
    <button class="btn btn-sm btn-danger"><i class="fas fa-trash"></i> Excluir</button>
</form>
