{% extends 'base.html.twig' %}

{% block title %}{{ nacionalidade.nome }} - {{ parent() }}{% endblock %}

{% block content %}
<div class="container-fluid">
    {% include '_partials/breadcrumb.html.twig' with {
        'items': [
            {'label': 'Tipos', 'url': path('app_dashboard_tipos_index')},
            {'label': 'Lista de Nacionalidades', 'url': path('app_nacionalidade_index')}
        ],
        'current': 'Ver Nacionalidade'
    } %}
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h1><i class="fas fa-flag"></i> {{ nacionalidade.nome }}</h1>
                <div>
                    <a href="{{ path('app_nacionalidade_edit', {'id': nacionalidade.id}) }}" class="btn btn-warning">
                        <i class="fas fa-edit"></i> Editar Nacionalidade
                    </a>
                    <a href="{{ path('app_nacionalidade_index') }}" class="btn btn-secondary ms-2">
                        <i class="fas fa-arrow-left"></i> Voltar
                    </a>
                </div>
            </div>

            <div class="card">
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <table class="table table-borderless">
                                <tr>
                                    <th width="150">ID:</th>
                                    <td>{{ nacionalidade.id }}</td>
                                </tr>
                                <tr>
                                    <th>Nome:</th>
                                    <td><strong>{{ nacionalidade.nome }}</strong></td>
                                </tr>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
