<div class="card mb-4">
    <div class="card-header">
        <h3><i class="fas fa-handshake"></i> Dados do Sócio</h3>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-4">{{ form_row(form.percentualParticipacao) }}</div>
            <div class="col-md-4">{{ form_row(form.dataEntrada) }}</div>
            <div class="col-md-4">{{ form_row(form.tipoSocio) }}</div>
        </div>
        <div class="row">
            <div class="col-md-12">{{ form_row(form.observacoes) }}</div>
        </div>
        <hr>
        <div class="row">
            <div class="col-md-4">
                <div class="form-check">
                    {{ form_widget(form.ativo) }}
                    {{ form_label(form.ativo) }}
                </div>
            </div>
        </div>
    </div>
</div>
