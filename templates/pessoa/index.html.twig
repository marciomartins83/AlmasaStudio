{% extends 'base.html.twig' %}

{% block title %}Pessoas{% endblock %}

{% block content %}
<div class="container-fluid">
    {% include '_partials/breadcrumb.html.twig' with {
        'current': 'Lista de Pessoas'
    } %}
    
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1><i class="fas fa-users"></i> Pessoas</h1>
        <a href="{{ path('app_pessoa_new') }}" class="btn btn-primary">
            <i class="fas fa-plus"></i> Nova Pessoa
        </a>
    </div>

    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Nome</th>
                            <th>Documento</th>
                            <th>Tipo(s)</th>
                            <th>Status</th>
                            <th class="text-end">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for pessoa in pessoas %}
                        {% set tiposNomes = {
                            'contratante': 'Contratante',
                            'fiador': 'Fiador',
                            'locador': 'Locador',
                            'corretor': 'Corretor',
                            'corretora': 'Corretora',
                            'pretendente': 'Pretendente'
                        } %}
                        <tr>
                            <td>{{ pessoa.entidade.idpessoa }}</td>
                            <td>{{ pessoa.entidade.nome }}</td>
                            <td>
                                {% if pessoa.cpf %}
                                    CPF: {{ pessoa.cpf }}
                                {% elseif pessoa.cnpj %}
                                    CNPJ: {{ pessoa.cnpj }}
                                {% else %}
                                    <span class="text-muted">Não informado</span>
                                {% endif %}
                            </td>
                            <td>
                                {% if pessoa.tipos is not empty %}
                                    {% for tipo in pessoa.tipos %}
                                        <div>{{ tiposNomes[tipo] ?? tipo|capitalize }}</div>
                                    {% endfor %}
                                {% else %}
                                    <span class="text-muted">Sem tipo</span>
                                {% endif %}
                            </td>
                            <td>
                                {% if pessoa.entidade.status %}
                                    <span class="badge bg-success">Ativo</span>
                                {% else %}
                                    <span class="badge bg-danger">Inativo</span>
                                {% endif %}
                            </td>
                            <td class="text-end">
                                <a href="{{ path('app_pessoa_show', {'id': pessoa.entidade.idpessoa}) }}" class="btn btn-sm btn-info" title="Ver">
                                    <i class="fas fa-eye"></i>
                                </a>
                                <a href="{{ path('app_pessoa_edit', {'id': pessoa.entidade.idpessoa}) }}" class="btn btn-sm btn-primary" title="Editar">
                                    <i class="fas fa-edit"></i>
                                </a>
                                {{ include('pessoa/_delete_form.html.twig', {'pessoa': pessoa.entidade}) }}
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="6" class="text-center">Nenhuma pessoa encontrada.</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %}
