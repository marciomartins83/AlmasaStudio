{% extends 'base.html.twig' %}

{% block title %}Perfil - Almasa{% endblock %}

{% block content %}
<div class="container my-4">
    {% include '_partials/breadcrumb.html.twig' with {
        'current': 'Perfil do Usuário'
    } %}
    
    <h2 class="almasa-text-gradient fs-3 fw-bold mb-4"><i class="fas fa-user"></i> Perfil do Usuário</h2>
    
    <div class="almasa-card p-4">
        <h3 class="fs-5 fw-semibold almasa-text-gradient">Informações Pessoais</h3>
        <p><strong>Nome:</strong> {{ user.nome ?? 'Não informado' }}</p>
        <p><strong>Email:</strong> {{ user.email }}</p>
    </div>

    <div class="almasa-card p-4 mt-4">
        <h3 class="fs-5 fw-semibold almasa-text-gradient">Alterar Senha</h3>
        <p>Para manter sua conta segura, use uma senha longa e aleatória.</p>
        <form method="post">
            <div class="almasa-form-group">
                <label class="almasa-form-label">Senha Atual</label>
                <input type="password" class="almasa-form-control" required>
            </div>
            <div class="almasa-form-group">
                <label class="almasa-form-label">Nova Senha</label>
                <input type="password" class="almasa-form-control" required>
            </div>
            <div class="almasa-form-group">
                <label class="almasa-form-label">Confirmar Senha</label>
                <input type="password" class="almasa-form-control" required>
            </div>
            <button type="submit" class="btn btn-almasa-primary">Salvar</button>
        </form>
    </div>

    <div class="almasa-card p-4 mt-4">
        <h3 class="fs-5 fw-semibold almasa-text-gradient">Sessões Ativas</h3>
        <p>Gerencie suas sessões abertas em outros dispositivos.</p>
        <button class="btn btn-almasa-outline">Encerrar todas as sessões</button>
    </div>

    <div class="almasa-card p-4 mt-4">
        <h3 class="fs-5 fw-semibold almasa-text-gradient text-danger">Excluir Conta</h3>
        <p>Se excluir sua conta, todos os seus dados serão removidos permanentemente.</p>
        <button class="btn btn-almasa-secondary">Excluir Conta</button>
    </div>
</div>
{% endblock %}
